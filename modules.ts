import { PI, calculateCircumferance } from "./math/circle";
import { calculateRectangle } from "./math/rectangle";
console.log(PI);
console.log(calculateCircumferance(4));
console.log(calculateRectangle(3, 4));
